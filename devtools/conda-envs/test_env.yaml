name: test
channels:
  - conda-forge
  - omnia
  - anaconda
  - pytorch
  - openeye
dependencies:
    # Base depends
  - python
  - pip
  - tqdm
  - openmmtools
  - perses #this should install _all_ simulation dependencies
  - pytorch
  - torchvision
  - cudatoolkit

    # Testing
  - pytest
  - pytest-cov
  - codecov
  - flake8

  # Pip-only installs
  # - pip:
  #  - codecov

